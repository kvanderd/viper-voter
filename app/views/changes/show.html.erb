<%= title "#{@change.name}" %>

<div class="row">
	<div class="col-sm-7">
		<%= render "changes/change" %>
	</div>

	<div class="col-sm-5">
		<%= render "changes/vote" %>
		<%= render "changes/repersenatives_holder" %>
	</div>
</div>

<div class="row">
	<div class="col-sm-10 col-sm-offset-1">
		<%= render "changes/stats" %>
	</div>
</div>

<h2 class="page-header text-center">Join the Discussion</h2>
	<%= render "comments/form" %>


	<div id="added_comments" class="col-sm-8 col-sm-offset-2"></div>

<div id="comments">
	<%= render @change.comments.reverse %>
</div>


