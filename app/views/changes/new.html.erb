<div class="row">
	<div class="col-md-6 text-center change_wrapper">
	<%= form_for @change, :html => {:role => "form", multipart: true} do |t| %>
	<%= render "shared/error_messages", :target => @change %>
	<div class="form-group">
		<%= t.label :name %>
		<%= t.text_field :name, class: "form-control", autofocus: true, required: true %>
	</div>
	<div class="form-group">
		<%= t.label :description %>
		<%= t.text_area :description, class: "form-control", required: true %>
	</div>
	<div class="form-group btn btn-file">
	 <%= t.file_field :picture, required: true %>
	</div>
	<h3>Select a Category</h3>
	<% @categories.each do |cat| %>
		<%= check_box_tag("change[category_id][]", cat.id) %> <%= cat.name %>
	<% end %>
<br />
<br />
	<p><%= t.submit class: "btn btn-default"%></p>
	<% end %>
</div>

	<div class="col-md-6 text-center">
		<h3>Preview</h3>
		<h2><div id="name"></div></h2>
		<div id="description"></div>

	</div>
</div>

<!-- upload an image -->

<!-- %ul
- @languages.each do |language|

%li
= check_box_tag "user[language_ids][]", campaign.id, @user.languages.include?(language)
= language.name
= submit_tag "Update" -->